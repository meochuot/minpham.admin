# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting
on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      API_URI: ${{ secrets.DEV_API_URI }}
      APP_MOUNT_URI: ${{ secrets.DEV_APP_MOUNT_URI }}
      STATIC_URL: ${{ secrets.DEV_STATIC_URL }}
      SENTRY_DSN: ${{ secrets.DEV_SENTRY_DSN }}
      IS_CLOUD_INSTANCE: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: 14
      - run: npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.DEV_FIREBASE_SERVICE_ACCOUNT }}'
          channelId: live
          projectId: '${{ secrets.DEV_PROJECT_ID }}'
          target: '${{ secrets.DEV_PROJECT_TARGET }}'
